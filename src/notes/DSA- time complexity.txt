Time complexity and space complexity


Time complexity : It is used to measure efficiency of alogorithm in terms  of speed, as the input size grows.

note : Time complexity its not equal to time taken by code.. its not code ruunnig this much time in your machine.

always looking for worst case scenario


Big O notation describes the upper bound of an algorithm's time or space complexity, indicating how its performance scales with the input size (n). The common orders of Big O notation, from most efficient to least efficient, are as follows:

O(1) - Constant Time:
The execution time remains constant regardless of the input size. Examples include accessing an array element by index or a hash table lookup.

O(log n) - Logarithmic Time:
The execution time grows logarithmically with the input size. This typically occurs in algorithms that divide the problem in half with each step, such as binary search.

O(n) - Linear Time:
The execution time grows linearly with the input size. This means if the input doubles, the time taken roughly doubles. Examples include iterating through a list or searching for an element in an unsorted array.

O(n log n) - Linearithmic Time:
The execution time grows in proportion to n times the logarithm of n. This is often seen in efficient sorting algorithms like Merge Sort or Quick Sort. 

O(n²) - Quadratic Time:
The execution time grows quadratically with the input size. If the input doubles, the time taken quadruples. This is characteristic of algorithms with nested loops that process the entire input for each element, such as Bubble Sort or insertion sort.

O(2ⁿ) - Exponential Time:
The execution time grows exponentially with the input size. These algorithms are generally impractical for large inputs. Examples include certain brute-force search algorithms.

O(n!) - Factorial Time:
The execution time grows factorially with the input size. This represents the slowest growth rate among common Big O notations and is typically only feasible for very small inputs. An example is the Traveling Salesperson Problem solved by brute force.

When analyzing an algorithm's complexity, the highest order term dominates as the input size 'n' becomes large, and constants or lower-order terms are typically disregarded in Big O notation.





Space complexity--- means how much extra spacing its using


The expression (O(n^{3}+n+n^{2})) simplifies to (O(n^{3})) in Big O notation because Big O notation describes the asymptotic upper bound of a function's growth rate. When multiple terms are present, the term with the highest growth rate dominates the function's behavior as \(n\) approaches infinity.            

   In this case:      (n^{3}) grows significantly faster than (n^{2}) and (n) as (n) becomes large. The lower-order terms ((n^{2}) and (n)) become negligible in comparison to (n^{3}) for sufficiently large values of (n).          Therefore, the overall complexity is determined by the dominant term, which is (n^{3}). This principle allows for the simplification of complex expressions to their highest-order term when using Big O notation.







